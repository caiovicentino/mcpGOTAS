name: "Gotas Commerce Payment Integration"
description: "Cryptocurrency payment gateway for USDT transactions"
startCommand:
  type: http
  command: "node"
  args: ["smithery-final-server.js"]
lazyLoading: true
transport: "http"
mcpEndpoint: "/mcp"
