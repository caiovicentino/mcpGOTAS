name: "Gotas Commerce Payment Integration"
description: "Cryptocurrency payment gateway for USDT transactions"
startCommand:
  type: http
  command: "node"
  args: ["smithery-compatible-final.js"]
lazyLoading: true
transport: "http"
mcpEndpoint: "/mcp"
