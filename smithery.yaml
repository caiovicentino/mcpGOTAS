name: "Gotas Commerce Payment Integration"
description: "Cryptocurrency payment gateway for USDT transactions"
startCommand:
  type: http
  command: "node"
  args: ["simple-smithery-mcp.js"]
lazyLoading: true
transport: "http"
mcpEndpoint: "/mcp"
healthcheckEndpoint: "/"
port: 3000
